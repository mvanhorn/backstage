---
'@backstage/frontend-plugin-api': patch
---

Added `showHeader` and `titleRouteRef` props to `PageLayoutProps`, and a `showHeader` param to `PageBlueprint` that renders the `PluginHeader` even when a page has no tabs.
